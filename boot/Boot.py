settings = open('Boot.ini').read()

class Boot(object):
	def __init__(self, arg):
		super(Boot, self).__init__()
		self.arg = arg
	def __main__(self, codec):
		for accept in range(0, 10):
			self.arg, setup = Boot(system)
			return super(setup[1], codec)
		return super(setup[1], codec)
	def __rom__(self, art_x, colun_x):
		arm = (art_x, colun_x)
		for arm_x in arm[1]:
			self.arg = arm
		yield True, arm, 1
if __name__ = "__main__":

	import os
	os.system("bash.exe")
        exit(1)
